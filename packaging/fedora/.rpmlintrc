# rpmlint configuration for speak-to-ai

Filters = [
    # Source0 is a local vendored tarball, not a URL
    ".*invalid-url Source0.*",
    # Locale warning in CI container (not our issue)
    ".*specfile-warning.*setlocale.*",
]
